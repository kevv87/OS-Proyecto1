name: Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build Decoder
        run: |
          cd decoder
          make
      - name: Build Encoder
        run: |
          cd encoder
          make
      - name: Build Visualizer
        run: |
          cd visualizer
          make